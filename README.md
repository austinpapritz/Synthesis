Look into using UseEffect to manage state changes for amplitude and frequency

May need to consider putting synth into it's own component

! Currently, setStates are not updating immediately !

State may need to be set in parent, with the value being passed onto the child
